import { SearchPresetsPipe } from './search-presets.pipe';

describe('SearchPresetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPresetsPipe();
    expect(pipe).toBeTruthy();
  });
});
